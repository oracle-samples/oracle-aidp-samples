import * as model from '../model';
/**
* Derived model for schema of a ADW external catalog.
*/
export interface AdwSchema extends model.Schema {
    "entityType": string;
}
export declare namespace AdwSchema {
    function getJsonObj(obj: AdwSchema, isParentJsonObj?: boolean): object;
    const entityType = "ADW";
    function getDeserializedJsonObj(obj: AdwSchema, isParentJsonObj?: boolean): object;
}
