import * as model from '../model';
/**
* User-created cluster.
*/
export interface AgentFlowCompute extends model.Cluster {
    "sourceApi": string;
}
export declare namespace AgentFlowCompute {
    function getJsonObj(obj: AgentFlowCompute, isParentJsonObj?: boolean): object;
    const sourceApi = "AGENT_FLOW_COMPUTE";
    function getDeserializedJsonObj(obj: AgentFlowCompute, isParentJsonObj?: boolean): object;
}
