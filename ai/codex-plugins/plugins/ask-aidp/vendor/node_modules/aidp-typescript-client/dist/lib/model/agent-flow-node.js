"use strict";
// Copyright (c) 2026, Oracle and/or its affiliates.  All rights reserved.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgentFlowNode = void 0;
const model = __importStar(require("../model"));
const common = require("oci-common");
var AgentFlowNode;
(function (AgentFlowNode) {
    function getJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {
            'inputs': obj.inputs ?
                obj.inputs.map((item) => { return model.NodeInput.getJsonObj(item); })
                : undefined,
            'outputs': obj.outputs ?
                obj.outputs.map((item) => { return model.NodeOutput.getJsonObj(item); })
                : undefined,
            'validationErrors': obj.validationErrors ?
                obj.validationErrors.map((item) => { return model.ValidationError.getJsonObj(item); })
                : undefined,
        });
        if (obj && "type" in obj && obj.type) {
            switch (obj.type) {
                case "START_NODE":
                    return model.StartNode.getJsonObj(jsonObj, true);
                case "HTTP_TOOL":
                    return model.HttpToolNode.getJsonObj(jsonObj, true);
                case "CUSTOM_TOOL":
                    return model.CustomToolNode.getJsonObj(jsonObj, true);
                case "RAG_TOOL":
                    return model.RagToolNode.getJsonObj(jsonObj, true);
                case "HUMAN_IN_THE_LOOP":
                    return model.HumanInTheLoopNode.getJsonObj(jsonObj, true);
                case "AGENT":
                    return model.AgentNode.getJsonObj(jsonObj, true);
                case "MCP_TOOL":
                    return model.McpToolNode.getJsonObj(jsonObj, true);
                case "EXTERNAL_AGENT":
                    return model.ExternalAgentNode.getJsonObj(jsonObj, true);
                case "SQL_TOOL":
                    return model.SqlToolNode.getJsonObj(jsonObj, true);
                case "SUPERVISOR_AGENT":
                    return model.SupervisorAgentNode.getJsonObj(jsonObj, true);
                case "NESTED_AGENT_FLOW":
                    return model.NestedAgentFlowNode.getJsonObj(jsonObj, true);
                case "PROMPT_TOOL":
                    return model.PromptToolNode.getJsonObj(jsonObj, true);
                case "GUARDRAILS":
                    return model.GuardrailNode.getJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.type}`);
            }
        }
        return jsonObj;
    }
    AgentFlowNode.getJsonObj = getJsonObj;
    ;
    function getDeserializedJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {
            'inputs': obj.inputs ?
                obj.inputs.map((item) => { return model.NodeInput.getDeserializedJsonObj(item); })
                : undefined,
            'outputs': obj.outputs ?
                obj.outputs.map((item) => { return model.NodeOutput.getDeserializedJsonObj(item); })
                : undefined,
            'validationErrors': obj.validationErrors ?
                obj.validationErrors.map((item) => { return model.ValidationError.getDeserializedJsonObj(item); })
                : undefined,
        });
        if (obj && "type" in obj && obj.type) {
            switch (obj.type) {
                case "START_NODE":
                    return model.StartNode.getDeserializedJsonObj(jsonObj, true);
                case "HTTP_TOOL":
                    return model.HttpToolNode.getDeserializedJsonObj(jsonObj, true);
                case "CUSTOM_TOOL":
                    return model.CustomToolNode.getDeserializedJsonObj(jsonObj, true);
                case "RAG_TOOL":
                    return model.RagToolNode.getDeserializedJsonObj(jsonObj, true);
                case "HUMAN_IN_THE_LOOP":
                    return model.HumanInTheLoopNode.getDeserializedJsonObj(jsonObj, true);
                case "AGENT":
                    return model.AgentNode.getDeserializedJsonObj(jsonObj, true);
                case "MCP_TOOL":
                    return model.McpToolNode.getDeserializedJsonObj(jsonObj, true);
                case "EXTERNAL_AGENT":
                    return model.ExternalAgentNode.getDeserializedJsonObj(jsonObj, true);
                case "SQL_TOOL":
                    return model.SqlToolNode.getDeserializedJsonObj(jsonObj, true);
                case "SUPERVISOR_AGENT":
                    return model.SupervisorAgentNode.getDeserializedJsonObj(jsonObj, true);
                case "NESTED_AGENT_FLOW":
                    return model.NestedAgentFlowNode.getDeserializedJsonObj(jsonObj, true);
                case "PROMPT_TOOL":
                    return model.PromptToolNode.getDeserializedJsonObj(jsonObj, true);
                case "GUARDRAILS":
                    return model.GuardrailNode.getDeserializedJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.type}`);
            }
        }
        return jsonObj;
    }
    AgentFlowNode.getDeserializedJsonObj = getDeserializedJsonObj;
})(AgentFlowNode || (exports.AgentFlowNode = AgentFlowNode = {}));
//# sourceMappingURL=agent-flow-node.js.map