import * as model from '../model';
/**
* Session Metrics data for a agentFlow.
*/
export interface AgentFlowSessionMetric {
    /**
    * The total number of sessions for the agent flow Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalSessions': number;
    /**
    * The number of successful sessions for the agent flow Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'successfulSessions': number;
    /**
    * The number of failed sessions for the agent flow Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'failedSessions': number;
    /**
    * The total token usage for all sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalTokenUsage'?: number;
    /**
    * The total input token usage count Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalInputTokenCount'?: number;
    /**
    * The total input token usage count Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalOutputTokenCount'?: number;
    /**
    * The average total token usage by session Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'avgTotalToken'?: number;
    /**
    * The average input token usage by session Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'avgInputToken'?: number;
    /**
    * The average output token usage by session Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'avgOutputToken'?: number;
    /**
    * The average duration of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'avgSessionDuration'?: number;
    /**
    * The min duration of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'minSessionDuration'?: number;
    /**
    * The max duration of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'maxSessionDuration'?: number;
    /**
    * The 50th percentile duration of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'sessionDurationP50'?: number;
    /**
    * The 95th percentile duration of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'sessionDurationP95'?: number;
    /**
    * The average Latency of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'avgSessionLatency'?: number;
    /**
    * The min Latency of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'minSessionLatency'?: number;
    /**
    * The max Latency of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'maxSessionLatency'?: number;
    /**
    * The 50th percentile Latency of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'sessionLatencyP50'?: number;
    /**
    * The 95th percentile Latency of sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'sessionLatencyP95'?: number;
    /**
    * The total requests for all sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalSessionRequests'?: number;
    /**
    * The successful requests for all sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'successfulSessionRequests'?: number;
    /**
    * The failed requests for all sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'failedSessionRequests'?: number;
    /**
    * The total error count for all sessions Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'totalErrorCount'?: number;
    /**
    * The user error count Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'userErrorCount'?: number;
    /**
    * The system error count Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'systemErrorCount'?: number;
    'range'?: model.AgentFlowSessionMetricsRange;
    /**
    * Ordered list of time-series. Each entry is keyed by the same name as the KPI it represents.
*
    */
    'dataPoints'?: Array<model.KeyedTimeSeries>;
}
export declare namespace AgentFlowSessionMetric {
    function getJsonObj(obj: AgentFlowSessionMetric): object;
    function getDeserializedJsonObj(obj: AgentFlowSessionMetric): object;
}
