/**
* Service provider information for an agent.
*/
export interface AgentProvider {
    /**
    * The name of the agent provider's organization.
    */
    'organization'?: string;
    /**
    * A URL for the agent provider's website or documentation.
    */
    'url'?: string;
}
export declare namespace AgentProvider {
    function getJsonObj(obj: AgentProvider): object;
    function getDeserializedJsonObj(obj: AgentProvider): object;
}
