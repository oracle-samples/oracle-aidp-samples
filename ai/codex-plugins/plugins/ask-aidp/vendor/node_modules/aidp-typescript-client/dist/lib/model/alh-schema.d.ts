import * as model from '../model';
/**
* Derived model for schema of a ALH external catalog.
*/
export interface AlhSchema extends model.Schema {
    "entityType": string;
}
export declare namespace AlhSchema {
    function getJsonObj(obj: AlhSchema, isParentJsonObj?: boolean): object;
    const entityType = "ALH";
    function getDeserializedJsonObj(obj: AlhSchema, isParentJsonObj?: boolean): object;
}
