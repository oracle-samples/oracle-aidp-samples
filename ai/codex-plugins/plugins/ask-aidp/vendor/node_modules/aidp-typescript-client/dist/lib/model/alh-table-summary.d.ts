import * as model from '../model';
/**
* Derived model for table summary of a ALH catalog.
*/
export interface AlhTableSummary extends model.TableSummary {
    "entityType": string;
}
export declare namespace AlhTableSummary {
    function getJsonObj(obj: AlhTableSummary, isParentJsonObj?: boolean): object;
    const entityType = "ALH";
    function getDeserializedJsonObj(obj: AlhTableSummary, isParentJsonObj?: boolean): object;
}
