import * as model from '../model';
/**
* Derived model for table of a ALH catalog.
*/
export interface AlhTable extends model.Table {
    "entityType": string;
}
export declare namespace AlhTable {
    function getJsonObj(obj: AlhTable, isParentJsonObj?: boolean): object;
    const entityType = "ALH";
    function getDeserializedJsonObj(obj: AlhTable, isParentJsonObj?: boolean): object;
}
