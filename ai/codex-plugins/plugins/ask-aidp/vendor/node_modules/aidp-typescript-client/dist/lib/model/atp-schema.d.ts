import * as model from '../model';
/**
* Derived model for schema of a ATP external catalog.
*/
export interface AtpSchema extends model.Schema {
    "entityType": string;
}
export declare namespace AtpSchema {
    function getJsonObj(obj: AtpSchema, isParentJsonObj?: boolean): object;
    const entityType = "ATP";
    function getDeserializedJsonObj(obj: AtpSchema, isParentJsonObj?: boolean): object;
}
