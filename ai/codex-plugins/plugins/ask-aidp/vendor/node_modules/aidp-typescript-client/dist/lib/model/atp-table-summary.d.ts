import * as model from '../model';
/**
* Derived model for table summary of a ATP catalog.
*/
export interface AtpTableSummary extends model.TableSummary {
    "entityType": string;
}
export declare namespace AtpTableSummary {
    function getJsonObj(obj: AtpTableSummary, isParentJsonObj?: boolean): object;
    const entityType = "ATP";
    function getDeserializedJsonObj(obj: AtpTableSummary, isParentJsonObj?: boolean): object;
}
