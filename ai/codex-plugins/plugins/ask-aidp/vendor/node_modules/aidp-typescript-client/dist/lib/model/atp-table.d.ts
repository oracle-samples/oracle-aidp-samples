import * as model from '../model';
/**
* Derived model for table of a ATP catalog.
*/
export interface AtpTable extends model.Table {
    "entityType": string;
}
export declare namespace AtpTable {
    function getJsonObj(obj: AtpTable, isParentJsonObj?: boolean): object;
    const entityType = "ATP";
    function getDeserializedJsonObj(obj: AtpTable, isParentJsonObj?: boolean): object;
}
