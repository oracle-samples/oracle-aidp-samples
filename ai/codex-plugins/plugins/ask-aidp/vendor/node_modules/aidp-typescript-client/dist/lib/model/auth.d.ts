/**
* agent flow to communicate with MCP using the defined auth mode
*/
export interface Auth {
    "authType": string;
}
export declare namespace Auth {
    function getJsonObj(obj: Auth): object;
    function getDeserializedJsonObj(obj: Auth): object;
}
