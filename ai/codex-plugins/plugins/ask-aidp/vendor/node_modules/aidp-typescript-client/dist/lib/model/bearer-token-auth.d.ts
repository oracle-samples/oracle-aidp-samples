import * as model from '../model';
/**
* Auth configuration while using bearer token
*/
export interface BearerTokenAuth extends model.Auth {
    /**
    * The bearer token used for auth
    */
    'token'?: string;
    "authType": string;
}
export declare namespace BearerTokenAuth {
    function getJsonObj(obj: BearerTokenAuth, isParentJsonObj?: boolean): object;
    const authType = "BEARER_TOKEN";
    function getDeserializedJsonObj(obj: BearerTokenAuth, isParentJsonObj?: boolean): object;
}
