import * as model from '../model';
/**
* Information about a cluster permission.
*/
export interface ClusterPermissionSummary {
    /**
    * OCID of user/group and name in case of role.
    */
    'grantee': string;
    /**
    * Simplified name of the grantee.
    */
    'granteeName'?: string;
    /**
    * Type of grantee.
    */
    'granteeType': model.GranteeType;
    /**
    * Selected permissions for a cluster.
    */
    'granteePermissions': Array<ClusterPermissionSummary.GranteePermissions>;
}
export declare namespace ClusterPermissionSummary {
    enum GranteePermissions {
        Read = "READ",
        Use = "USE",
        Admin = "ADMIN",
        /**
        * This value is used if a service returns a value for this enum that is not recognized by this
        * version of the SDK.
        */
        UnknownValue = "UNKNOWN_VALUE"
    }
    function getJsonObj(obj: ClusterPermissionSummary): object;
    function getDeserializedJsonObj(obj: ClusterPermissionSummary): object;
}
