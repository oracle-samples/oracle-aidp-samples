import * as model from '../model';
/**
* Cluster runtime configurations.
*/
export interface ClusterRuntimeConfig {
    /**
    * Init script paths that are run in the order of definition.
    */
    'initScripts'?: Array<model.InitScriptPath>;
    "type": string;
}
export declare namespace ClusterRuntimeConfig {
    function getJsonObj(obj: ClusterRuntimeConfig): object;
    function getDeserializedJsonObj(obj: ClusterRuntimeConfig): object;
}
