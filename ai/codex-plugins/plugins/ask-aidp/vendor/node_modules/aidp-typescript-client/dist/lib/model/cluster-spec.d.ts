import * as model from '../model';
/**
* Cluster specification snapshot of a job.
*/
export interface ClusterSpec {
    /**
    * The key of the cluster.
    */
    'existingClusterKey'?: string;
    'newCluster'?: model.NewClusterConfiguration;
    'libraries'?: model.Libraries;
}
export declare namespace ClusterSpec {
    function getJsonObj(obj: ClusterSpec): object;
    function getDeserializedJsonObj(obj: ClusterSpec): object;
}
