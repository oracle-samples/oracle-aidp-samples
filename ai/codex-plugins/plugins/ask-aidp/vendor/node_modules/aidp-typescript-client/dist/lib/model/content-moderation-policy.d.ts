import * as model from '../model';
/**
* Content moderation safety policy
*/
export interface ContentModerationPolicy extends model.SafetyPolicy {
    /**
    * Content moderation categories and their configurations
    */
    'categories'?: Array<model.ContentModerationCategoryConfig>;
    "policyType": string;
}
export declare namespace ContentModerationPolicy {
    function getJsonObj(obj: ContentModerationPolicy, isParentJsonObj?: boolean): object;
    const policyType = "CONTENT_MODERATION";
    function getDeserializedJsonObj(obj: ContentModerationPolicy, isParentJsonObj?: boolean): object;
}
