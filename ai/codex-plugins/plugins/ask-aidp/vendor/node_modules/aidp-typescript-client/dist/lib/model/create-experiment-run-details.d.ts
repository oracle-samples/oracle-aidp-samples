import * as model from '../model';
/**
* Details of run to create.
*/
export interface CreateExperimentRunDetails {
    /**
    * Name of the run.
    */
    'runName'?: string;
    /**
    * ID of the associated experiment.
    */
    'experimentId'?: string;
    /**
    * Unix timestamp in milliseconds when the run started. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
    */
    'startTime'?: number;
    /**
    * Tags to log.
    */
    'tags'?: Array<model.ExperimentRunTag>;
}
export declare namespace CreateExperimentRunDetails {
    function getJsonObj(obj: CreateExperimentRunDetails): object;
    function getDeserializedJsonObj(obj: CreateExperimentRunDetails): object;
}
