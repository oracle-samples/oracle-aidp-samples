import * as model from '../model';
/**
* Results of an endpoint search. Contains EndpointSummary items and other information such as metadata.
*/
export interface DacEndpointCollection {
    /**
    * List of endpoints.
    */
    'items': Array<model.DacEndpointSummary>;
}
export declare namespace DacEndpointCollection {
    function getJsonObj(obj: DacEndpointCollection): object;
    function getDeserializedJsonObj(obj: DacEndpointCollection): object;
}
