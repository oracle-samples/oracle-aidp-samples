import * as model from '../model';
/**
* DatasetInput
*/
export interface DatasetInput {
    'dataset': model.Dataset;
    /**
    * Tags for the dataset input.
    */
    'tags'?: Array<model.InputTag>;
}
export declare namespace DatasetInput {
    function getJsonObj(obj: DatasetInput): object;
    function getDeserializedJsonObj(obj: DatasetInput): object;
}
