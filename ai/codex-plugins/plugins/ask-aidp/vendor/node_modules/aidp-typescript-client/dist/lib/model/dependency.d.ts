/**
* Base class for all dependencies, including files, compute resources, and nested jobs.
*/
export interface Dependency {
    "type": string;
}
export declare namespace Dependency {
    function getJsonObj(obj: Dependency): object;
    function getDeserializedJsonObj(obj: Dependency): object;
}
