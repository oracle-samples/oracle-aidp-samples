import * as model from '../model';
/**
* Derived model for schema of a Exadata external catalog.
*/
export interface ExadataSchema extends model.Schema {
    "entityType": string;
}
export declare namespace ExadataSchema {
    function getJsonObj(obj: ExadataSchema, isParentJsonObj?: boolean): object;
    const entityType = "EXADATA";
    function getDeserializedJsonObj(obj: ExadataSchema, isParentJsonObj?: boolean): object;
}
