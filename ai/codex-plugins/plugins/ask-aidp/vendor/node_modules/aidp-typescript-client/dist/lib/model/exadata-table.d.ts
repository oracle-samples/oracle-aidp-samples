import * as model from '../model';
/**
* Derived model for table of a Exadata catalog.
*/
export interface ExadataTable extends model.Table {
    "entityType": string;
}
export declare namespace ExadataTable {
    function getJsonObj(obj: ExadataTable, isParentJsonObj?: boolean): object;
    const entityType = "EXADATA";
    function getDeserializedJsonObj(obj: ExadataTable, isParentJsonObj?: boolean): object;
}
