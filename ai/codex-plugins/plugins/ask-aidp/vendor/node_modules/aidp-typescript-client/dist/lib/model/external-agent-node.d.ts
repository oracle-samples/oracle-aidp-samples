import * as model from '../model';
/**
* An External Agent Node in an Agent Flow.
*/
export interface ExternalAgentNode extends model.AgentFlowNode {
    /**
    * Custom prompt written by the user defining the agent\u2019s goal(s) and what tools the agent has access to
    */
    'instructions'?: string;
    /**
    * Extra configuration for the external agent node.
    */
    'externalAgentConfig'?: {
        [key: string]: string;
    };
    "type": string;
}
export declare namespace ExternalAgentNode {
    function getJsonObj(obj: ExternalAgentNode, isParentJsonObj?: boolean): object;
    const type = "EXTERNAL_AGENT";
    function getDeserializedJsonObj(obj: ExternalAgentNode, isParentJsonObj?: boolean): object;
}
