/**
* Git configuration used when source is GIT_PROVIDER.
*/
export interface GitConfig {
    /**
    * Git provider.
    */
    'provider'?: GitConfig.Provider;
    /**
    * Git credential to access the repository.
    */
    'credential'?: string;
    /**
    * Git repository URL.
    */
    'repositoryUrl'?: string;
    /**
    * Git branch path.
    */
    'branch'?: string;
}
export declare namespace GitConfig {
    enum Provider {
        Github = "GITHUB",
        Bitbucket = "BITBUCKET",
        Gitlab = "GITLAB",
        OciDevops = "OCI_DEVOPS",
        /**
        * This value is used if a service returns a value for this enum that is not recognized by this
        * version of the SDK.
        */
        UnknownValue = "UNKNOWN_VALUE"
    }
    function getJsonObj(obj: GitConfig): object;
    function getDeserializedJsonObj(obj: GitConfig): object;
}
