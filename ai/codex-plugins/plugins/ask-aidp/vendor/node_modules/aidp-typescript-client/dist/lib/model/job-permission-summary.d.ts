import * as model from '../model';
/**
* Information about a job permission.
*/
export interface JobPermissionSummary {
    /**
    * The OCID of user/group and name in case of role.
    */
    'grantee': string;
    /**
    * The simplified name of the grantee.
    */
    'granteeName'?: string;
    /**
    * The type of grantee.
    */
    'granteeType': model.GranteeType;
    /**
    * The selected permissions for a Job.
    */
    'granteePermissions': Array<JobPermissionSummary.GranteePermissions>;
}
export declare namespace JobPermissionSummary {
    enum GranteePermissions {
        Read = "READ",
        Use = "USE",
        Manage = "MANAGE",
        Admin = "ADMIN",
        /**
        * This value is used if a service returns a value for this enum that is not recognized by this
        * version of the SDK.
        */
        UnknownValue = "UNKNOWN_VALUE"
    }
    function getJsonObj(obj: JobPermissionSummary): object;
    function getDeserializedJsonObj(obj: JobPermissionSummary): object;
}
