import * as model from '../model';
/**
* List of job runs.
*/
export interface JobRunCollection {
    /**
    * List of job runs.
    */
    'items': Array<model.JobRunSummary>;
}
export declare namespace JobRunCollection {
    function getJsonObj(obj: JobRunCollection): object;
    function getDeserializedJsonObj(obj: JobRunCollection): object;
}
