import * as model from '../model';
/**
* Derived model for topic of a Kafka external catalog.
*/
export interface KafkaTopicSchemaSummary extends model.SchemaSummary {
    /**
    * The number of partitions in the Kafka topic.
    */
    'partitions'?: string;
    /**
    * The replication factor.
    */
    'replicationFactor'?: string;
    "entityType": string;
}
export declare namespace KafkaTopicSchemaSummary {
    function getJsonObj(obj: KafkaTopicSchemaSummary, isParentJsonObj?: boolean): object;
    const entityType = "KAFKA_TOPIC";
    function getDeserializedJsonObj(obj: KafkaTopicSchemaSummary, isParentJsonObj?: boolean): object;
}
