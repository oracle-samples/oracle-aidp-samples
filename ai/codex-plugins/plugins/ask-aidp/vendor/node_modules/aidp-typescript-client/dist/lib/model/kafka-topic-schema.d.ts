import * as model from '../model';
/**
* Derived model for topic of a Kafka external catalog.
*/
export interface KafkaTopicSchema extends model.Schema {
    /**
    * The number of partitions in the Kafka topic.
    */
    'partitions'?: string;
    /**
    * The replication factor.
    */
    'replicationFactor'?: string;
    /**
    * Bootstrap servers for the Kafka topic.
    */
    'bootstrapServers'?: string;
    /**
    * The details of the partitions in Kafka topic.
    */
    'partitionDetails': string;
    "entityType": string;
}
export declare namespace KafkaTopicSchema {
    function getJsonObj(obj: KafkaTopicSchema, isParentJsonObj?: boolean): object;
    const entityType = "KAFKA_TOPIC";
    function getDeserializedJsonObj(obj: KafkaTopicSchema, isParentJsonObj?: boolean): object;
}
