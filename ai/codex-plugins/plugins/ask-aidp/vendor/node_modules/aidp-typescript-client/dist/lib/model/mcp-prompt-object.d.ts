import * as model from '../model';
/**
* Prompt exposed by an MCP server.
*/
export interface McpPromptObject extends model.McpObject {
    /**
    * prompt persisted within an MCP server.
    */
    'prompt'?: string;
    "objectType": string;
}
export declare namespace McpPromptObject {
    function getJsonObj(obj: McpPromptObject, isParentJsonObj?: boolean): object;
    const objectType = "PROMPT";
    function getDeserializedJsonObj(obj: McpPromptObject, isParentJsonObj?: boolean): object;
}
