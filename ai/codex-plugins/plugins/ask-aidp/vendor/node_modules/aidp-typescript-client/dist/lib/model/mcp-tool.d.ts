import * as model from '../model';
/**
* Derived model for Mcp Tool
*/
export interface McpTool extends model.Tool {
    /**
    * The list of properties in the inputSchema, along with the default value and description of each property
    */
    'inputSchema'?: {
        [key: string]: any;
    };
    'toolConfig': model.McpToolConfiguration;
    "toolType": string;
}
export declare namespace McpTool {
    function getJsonObj(obj: McpTool, isParentJsonObj?: boolean): object;
    const toolType = "MCP";
    function getDeserializedJsonObj(obj: McpTool, isParentJsonObj?: boolean): object;
}
