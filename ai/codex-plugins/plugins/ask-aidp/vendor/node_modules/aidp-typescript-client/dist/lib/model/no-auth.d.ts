import * as model from '../model';
/**
* Auth configuration while using no auth mode
*/
export interface NoAuth extends model.Auth {
    "authType": string;
}
export declare namespace NoAuth {
    function getJsonObj(obj: NoAuth, isParentJsonObj?: boolean): object;
    const authType = "NO_AUTH";
    function getDeserializedJsonObj(obj: NoAuth, isParentJsonObj?: boolean): object;
}
