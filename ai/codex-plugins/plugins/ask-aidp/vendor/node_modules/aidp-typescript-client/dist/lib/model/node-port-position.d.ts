/**
 * UI position of an input or output port on a node.
**/
export declare enum NodePortPosition {
    Top = "TOP",
    Bottom = "BOTTOM",
    Right = "RIGHT",
    Left = "LEFT"
}
export declare namespace NodePortPosition {
    function getJsonObj(obj: NodePortPosition): NodePortPosition;
    function getDeserializedJsonObj(obj: NodePortPosition): NodePortPosition;
}
