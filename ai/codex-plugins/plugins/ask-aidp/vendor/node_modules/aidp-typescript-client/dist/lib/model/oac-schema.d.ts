import * as model from '../model';
/**
* Derived model for schema of an Oracle Analytics external catalog.
*/
export interface OacSchema extends model.Schema {
    "entityType": string;
}
export declare namespace OacSchema {
    function getJsonObj(obj: OacSchema, isParentJsonObj?: boolean): object;
    const entityType = "ORACLE_ANALYTICS";
    function getDeserializedJsonObj(obj: OacSchema, isParentJsonObj?: boolean): object;
}
