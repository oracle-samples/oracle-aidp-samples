import * as model from '../model';
/**
* Derived model for table of an Oracle Analytics catalog.
*/
export interface OacTable extends model.Table {
    "entityType": string;
}
export declare namespace OacTable {
    function getJsonObj(obj: OacTable, isParentJsonObj?: boolean): object;
    const entityType = "ORACLE_ANALYTICS";
    function getDeserializedJsonObj(obj: OacTable, isParentJsonObj?: boolean): object;
}
