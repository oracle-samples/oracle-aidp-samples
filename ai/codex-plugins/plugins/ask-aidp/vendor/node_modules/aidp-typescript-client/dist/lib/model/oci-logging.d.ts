import * as model from '../model';
/**
* OCI logging configuration.
*/
export interface OciLogging extends model.LoggingConfig {
    /**
    * Init script paths that are run in the order of definition.
    */
    'logGroups'?: Array<model.LogGroup>;
    "type": string;
}
export declare namespace OciLogging {
    function getJsonObj(obj: OciLogging, isParentJsonObj?: boolean): object;
    const type = "OCI_LOGGING";
    function getDeserializedJsonObj(obj: OciLogging, isParentJsonObj?: boolean): object;
}
