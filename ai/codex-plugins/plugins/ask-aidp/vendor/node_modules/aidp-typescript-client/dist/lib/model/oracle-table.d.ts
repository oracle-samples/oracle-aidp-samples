import * as model from '../model';
/**
* Derived model for table of a Oracle catalog.
*/
export interface OracleTable extends model.Table {
    "entityType": string;
}
export declare namespace OracleTable {
    function getJsonObj(obj: OracleTable, isParentJsonObj?: boolean): object;
    const entityType = "ORACLE";
    function getDeserializedJsonObj(obj: OracleTable, isParentJsonObj?: boolean): object;
}
