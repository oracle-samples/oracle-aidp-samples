import * as model from '../model';
/**
* The information about the assignees
*/
export interface PermissionAssignees {
    /**
    * Grantee type.
    */
    'type': model.GranteeType;
    /**
    * The names/ocids of the users, groups or roles.
    */
    'targets': Array<string>;
}
export declare namespace PermissionAssignees {
    function getJsonObj(obj: PermissionAssignees): object;
    function getDeserializedJsonObj(obj: PermissionAssignees): object;
}
