import * as model from '../model';
/**
* Playground cache for an Agent Flow session.
*/
export interface PlaygroundCache {
    /**
    * Map of variable name to value for this session's playground cache.
    */
    'variables': {
        [key: string]: model.SessionVariable;
    };
}
export declare namespace PlaygroundCache {
    function getJsonObj(obj: PlaygroundCache): object;
    function getDeserializedJsonObj(obj: PlaygroundCache): object;
}
