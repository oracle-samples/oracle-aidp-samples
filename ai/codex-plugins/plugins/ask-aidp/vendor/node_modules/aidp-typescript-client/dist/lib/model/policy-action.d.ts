/**
 * Action to take when policy is violated
**/
export declare enum PolicyAction {
    Block = "BLOCK",
    Inform = "INFORM",
    Mask = "MASK"
}
export declare namespace PolicyAction {
    function getJsonObj(obj: PolicyAction): PolicyAction;
    function getDeserializedJsonObj(obj: PolicyAction): PolicyAction;
}
