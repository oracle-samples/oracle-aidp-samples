/**
 * Scope of policy application
**/
export declare enum PolicyScope {
    UserRequest = "USER_REQUEST",
    AgentResponse = "AGENT_RESPONSE",
    Both = "BOTH"
}
export declare namespace PolicyScope {
    function getJsonObj(obj: PolicyScope): PolicyScope;
    function getDeserializedJsonObj(obj: PolicyScope): PolicyScope;
}
