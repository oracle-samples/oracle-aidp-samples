import * as model from '../model';
/**
* Derived model for Prompt Tool
*/
export interface PromptTool extends model.Tool {
    /**
    * The list of properties in the inputSchema, along with the default value and description of each property
    */
    'inputSchema'?: {
        [key: string]: any;
    };
    'toolConfig'?: model.PromptToolConfiguration;
    "toolType": string;
}
export declare namespace PromptTool {
    function getJsonObj(obj: PromptTool, isParentJsonObj?: boolean): object;
    const toolType = "PROMPT";
    function getDeserializedJsonObj(obj: PromptTool, isParentJsonObj?: boolean): object;
}
