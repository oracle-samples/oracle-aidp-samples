/**
* Queue configuration for job.
*/
export interface Queue {
    /**
    * True if job queue is enabled.
    */
    'isEnabled': boolean;
}
export declare namespace Queue {
    function getJsonObj(obj: Queue): object;
    function getDeserializedJsonObj(obj: Queue): object;
}
