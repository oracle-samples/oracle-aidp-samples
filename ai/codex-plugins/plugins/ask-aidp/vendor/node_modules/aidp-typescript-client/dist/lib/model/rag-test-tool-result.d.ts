import * as model from '../model';
/**
* Response when toolType = RAG.
*/
export interface RagTestToolResult extends model.TestToolResult {
    'result': model.RagResult;
    "toolType": string;
}
export declare namespace RagTestToolResult {
    function getJsonObj(obj: RagTestToolResult, isParentJsonObj?: boolean): object;
    const toolType = "RAG";
    function getDeserializedJsonObj(obj: RagTestToolResult, isParentJsonObj?: boolean): object;
}
