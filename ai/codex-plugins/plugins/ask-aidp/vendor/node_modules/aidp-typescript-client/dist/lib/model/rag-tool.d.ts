import * as model from '../model';
/**
* Derived model for RAG Tool
*/
export interface RagTool extends model.Tool {
    'inputSchema'?: model.RagToolInputSchema;
    'toolConfig'?: model.RagToolConfiguration;
    "toolType": string;
}
export declare namespace RagTool {
    function getJsonObj(obj: RagTool, isParentJsonObj?: boolean): object;
    const toolType = "RAG";
    function getDeserializedJsonObj(obj: RagTool, isParentJsonObj?: boolean): object;
}
