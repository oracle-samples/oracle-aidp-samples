import * as model from '../model';
/**
* Results of a listing Delta Share recipients. Contains summary information of recipients.
*/
export interface RecipientCollection {
    /**
    * List of recipients.
    */
    'items': Array<model.RecipientSummary>;
}
export declare namespace RecipientCollection {
    function getJsonObj(obj: RecipientCollection): object;
    function getDeserializedJsonObj(obj: RecipientCollection): object;
}
