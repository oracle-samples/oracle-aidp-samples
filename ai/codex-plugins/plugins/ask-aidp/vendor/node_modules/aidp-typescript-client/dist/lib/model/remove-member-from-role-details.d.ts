import * as model from '../model';
/**
* The details of revoking the role.
*/
export interface RemoveMemberFromRoleDetails {
    /**
    * The assignees on the role.
    */
    'assignees': Array<model.RoleAssignee>;
}
export declare namespace RemoveMemberFromRoleDetails {
    function getJsonObj(obj: RemoveMemberFromRoleDetails): object;
    function getDeserializedJsonObj(obj: RemoveMemberFromRoleDetails): object;
}
