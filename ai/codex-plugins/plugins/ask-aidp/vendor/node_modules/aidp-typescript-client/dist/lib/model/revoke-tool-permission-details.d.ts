import * as model from '../model';
/**
* The details of the permissions revoked from assignee for a tool.
*/
export interface RevokeToolPermissionDetails {
    'assignees': model.PermissionAssignees;
    /**
    * The permissions revoked from the assignee.
    */
    'permissions': Array<RevokeToolPermissionDetails.Permissions>;
    /**
    * The list of columns to be included for the revoking of permissions.
    */
    'includeColumns': Array<string>;
    /**
    * The list of columns to be excluded for the revoking of permissions.
    */
    'excludeColumns': Array<string>;
}
export declare namespace RevokeToolPermissionDetails {
    enum Permissions {
        Read = "READ",
        Manage = "MANAGE",
        Admin = "ADMIN"
    }
    function getJsonObj(obj: RevokeToolPermissionDetails): object;
    function getDeserializedJsonObj(obj: RevokeToolPermissionDetails): object;
}
