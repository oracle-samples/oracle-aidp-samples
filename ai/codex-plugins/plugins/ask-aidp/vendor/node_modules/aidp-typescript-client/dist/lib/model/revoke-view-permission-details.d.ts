import * as model from '../model';
/**
* The details of the permissions revoked from assignee for a view.
*/
export interface RevokeViewPermissionDetails {
    'assignees': model.PermissionAssignees;
    /**
    * The permissions revoked from the assignee.
    */
    'permissions': Array<RevokeViewPermissionDetails.Permissions>;
    /**
    * The list of columns to be included for the revoking of permissions.
    */
    'includeColumns': Array<string>;
    /**
    * The list of columns to be excluded for the revoking of permissions.
    */
    'excludeColumns': Array<string>;
}
export declare namespace RevokeViewPermissionDetails {
    enum Permissions {
        Select = "SELECT",
        Alter = "ALTER",
        Admin = "ADMIN"
    }
    function getJsonObj(obj: RevokeViewPermissionDetails): object;
    function getDeserializedJsonObj(obj: RevokeViewPermissionDetails): object;
}
