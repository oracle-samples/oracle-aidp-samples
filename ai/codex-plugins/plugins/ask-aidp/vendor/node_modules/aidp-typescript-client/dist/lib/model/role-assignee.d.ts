import * as model from '../model';
/**
* The user/principal/role the role can be assigned to.
*/
export interface RoleAssignee {
    /**
    * assignee type.
    */
    'type': model.RoleAssigneeType;
    /**
    * The OCID for a principal or role.
    */
    'target': string;
    /**
    * The name for a principal or role.
    */
    'targetName'?: string;
}
export declare namespace RoleAssignee {
    function getJsonObj(obj: RoleAssignee): object;
    function getDeserializedJsonObj(obj: RoleAssignee): object;
}
