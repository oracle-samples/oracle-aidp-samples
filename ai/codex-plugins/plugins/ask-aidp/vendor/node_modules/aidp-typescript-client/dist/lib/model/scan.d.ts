/**
* Single Client Access Name (SCAN) is the object with a fully-qualified domain name and a port number.
*/
export interface Scan {
    /**
    * A fully-qualified domain name (FQDN).
    */
    'fqdn'?: string;
    /**
    * Port number of the FQDN.
    */
    'port'?: string;
}
export declare namespace Scan {
    function getJsonObj(obj: Scan): object;
    function getDeserializedJsonObj(obj: Scan): object;
}
