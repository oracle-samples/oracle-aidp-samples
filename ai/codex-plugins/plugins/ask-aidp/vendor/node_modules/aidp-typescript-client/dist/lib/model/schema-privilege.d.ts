/**
 * The privilege for a schema. This privilege could be inherited from the object higher up in hierarchy.
**/
export declare enum SchemaPrivilege {
    Select = "SELECT",
    Manage = "MANAGE",
    Write = "WRITE",
    CreateView = "CREATE_VIEW",
    CreateVolume = "CREATE_VOLUME",
    CreateTable = "CREATE_TABLE",
    CreateKnowledgeBase = "CREATE_KNOWLEDGE_BASE",
    Admin = "ADMIN",
    /**
     * This value is used if a service returns a value for this enum that is not recognized by this
     * version of the SDK.
     */
    UnknownValue = "UNKNOWN_VALUE"
}
export declare namespace SchemaPrivilege {
    function getJsonObj(obj: SchemaPrivilege): SchemaPrivilege;
    function getDeserializedJsonObj(obj: SchemaPrivilege): SchemaPrivilege;
}
