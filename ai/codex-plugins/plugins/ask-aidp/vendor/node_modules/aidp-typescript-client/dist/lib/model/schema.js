"use strict";
// Copyright (c) 2026, Oracle and/or its affiliates.  All rights reserved.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Schema = void 0;
const model = __importStar(require("../model"));
const common = require("oci-common");
var Schema;
(function (Schema) {
    function getJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {});
        if (obj && "entityType" in obj && obj.entityType) {
            switch (obj.entityType) {
                case "ORACLE":
                    return model.OracleSchema.getJsonObj(jsonObj, true);
                case "ALH":
                    return model.AlhSchema.getJsonObj(jsonObj, true);
                case "ADW":
                    return model.AdwSchema.getJsonObj(jsonObj, true);
                case "KAFKA_TOPIC":
                    return model.KafkaTopicSchema.getJsonObj(jsonObj, true);
                case "ATP":
                    return model.AtpSchema.getJsonObj(jsonObj, true);
                case "STANDARD":
                    return model.StandardSchema.getJsonObj(jsonObj, true);
                case "ORACLE_ANALYTICS":
                    return model.OacSchema.getJsonObj(jsonObj, true);
                case "EXADATA":
                    return model.ExadataSchema.getJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.entityType}`);
            }
        }
        return jsonObj;
    }
    Schema.getJsonObj = getJsonObj;
    ;
    function getDeserializedJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {});
        if (obj && "entityType" in obj && obj.entityType) {
            switch (obj.entityType) {
                case "ORACLE":
                    return model.OracleSchema.getDeserializedJsonObj(jsonObj, true);
                case "ALH":
                    return model.AlhSchema.getDeserializedJsonObj(jsonObj, true);
                case "ADW":
                    return model.AdwSchema.getDeserializedJsonObj(jsonObj, true);
                case "KAFKA_TOPIC":
                    return model.KafkaTopicSchema.getDeserializedJsonObj(jsonObj, true);
                case "ATP":
                    return model.AtpSchema.getDeserializedJsonObj(jsonObj, true);
                case "STANDARD":
                    return model.StandardSchema.getDeserializedJsonObj(jsonObj, true);
                case "ORACLE_ANALYTICS":
                    return model.OacSchema.getDeserializedJsonObj(jsonObj, true);
                case "EXADATA":
                    return model.ExadataSchema.getDeserializedJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.entityType}`);
            }
        }
        return jsonObj;
    }
    Schema.getDeserializedJsonObj = getDeserializedJsonObj;
})(Schema || (exports.Schema = Schema = {}));
//# sourceMappingURL=schema.js.map