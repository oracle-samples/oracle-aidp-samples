import * as model from '../model';
/**
* A list of session summaries related to a workspace.
*/
export interface SessionCollection {
    /**
    * List of sessions.
    */
    'items': Array<model.SessionSummary>;
}
export declare namespace SessionCollection {
    function getJsonObj(obj: SessionCollection): object;
    function getDeserializedJsonObj(obj: SessionCollection): object;
}
