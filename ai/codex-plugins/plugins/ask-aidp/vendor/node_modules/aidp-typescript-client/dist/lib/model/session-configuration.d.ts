import * as model from '../model';
/**
* Session configuration for an agent flow
*/
export interface SessionConfiguration {
    /**
    * Map of session variable name \u2192 variable definition
    */
    'variables'?: {
        [key: string]: model.SessionVariableDetails;
    };
    'sessionRetentionConfig'?: model.SessionRetentionConfiguration;
}
export declare namespace SessionConfiguration {
    function getJsonObj(obj: SessionConfiguration): object;
    function getDeserializedJsonObj(obj: SessionConfiguration): object;
}
