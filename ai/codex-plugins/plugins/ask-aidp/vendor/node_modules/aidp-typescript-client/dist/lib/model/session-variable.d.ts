/**
* Session Variable for each Session Context.
*/
export interface SessionVariable {
    /**
    * Name of the Session Variable.
    */
    'name': string;
    /**
    * Value of this Session Variable for this session
    */
    'value'?: string;
}
export declare namespace SessionVariable {
    function getJsonObj(obj: SessionVariable): object;
    function getDeserializedJsonObj(obj: SessionVariable): object;
}
