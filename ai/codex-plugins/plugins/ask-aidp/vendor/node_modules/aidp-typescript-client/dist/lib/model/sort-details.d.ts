import * as model from '../model';
/**
* Object with sort criteria details
*/
export interface SortDetails {
    /**
    * Field name that needs to be sorted by.
    */
    'sortBy'?: model.SortFieldEnum;
    /**
    * Sort order for search results.
    */
    'sortOrder'?: SortDetails.SortOrder;
}
export declare namespace SortDetails {
    enum SortOrder {
        Asc = "ASC",
        Desc = "DESC"
    }
    function getJsonObj(obj: SortDetails): object;
    function getDeserializedJsonObj(obj: SortDetails): object;
}
