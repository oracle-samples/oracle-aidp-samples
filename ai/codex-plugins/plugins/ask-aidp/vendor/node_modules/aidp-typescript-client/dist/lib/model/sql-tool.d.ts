import * as model from '../model';
/**
* Derived model a SQL Tool
*/
export interface SqlTool extends model.Tool {
    /**
    * The list of properties in the inputSchema, along with the default value and description of each property
    */
    'inputSchema'?: {
        [key: string]: any;
    };
    'toolConfig'?: model.SqlToolConfiguration;
    "toolType": string;
}
export declare namespace SqlTool {
    function getJsonObj(obj: SqlTool, isParentJsonObj?: boolean): object;
    const toolType = "SQL";
    function getDeserializedJsonObj(obj: SqlTool, isParentJsonObj?: boolean): object;
}
