import * as model from '../model';
/**
* Derived model for schema of a standard catalog.
*/
export interface StandardSchemaSummary extends model.SchemaSummary {
    "entityType": string;
}
export declare namespace StandardSchemaSummary {
    function getJsonObj(obj: StandardSchemaSummary, isParentJsonObj?: boolean): object;
    const entityType = "STANDARD";
    function getDeserializedJsonObj(obj: StandardSchemaSummary, isParentJsonObj?: boolean): object;
}
