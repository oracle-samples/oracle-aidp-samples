import * as model from '../model';
/**
* Derived model for schema of a standard catalog.
*/
export interface StandardSchema extends model.Schema {
    "entityType": string;
}
export declare namespace StandardSchema {
    function getJsonObj(obj: StandardSchema, isParentJsonObj?: boolean): object;
    const entityType = "STANDARD";
    function getDeserializedJsonObj(obj: StandardSchema, isParentJsonObj?: boolean): object;
}
