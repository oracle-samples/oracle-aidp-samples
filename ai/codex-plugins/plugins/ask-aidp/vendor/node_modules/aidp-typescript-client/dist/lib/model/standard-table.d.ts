import * as model from '../model';
/**
* Derived model for table of a standard catalog.
*/
export interface StandardTable extends model.Table {
    "entityType": string;
}
export declare namespace StandardTable {
    function getJsonObj(obj: StandardTable, isParentJsonObj?: boolean): object;
    const entityType = "STANDARD";
    function getDeserializedJsonObj(obj: StandardTable, isParentJsonObj?: boolean): object;
}
