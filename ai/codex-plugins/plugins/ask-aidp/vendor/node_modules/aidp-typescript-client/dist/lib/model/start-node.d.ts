import * as model from '../model';
/**
* A starting point in an agent flow.
*/
export interface StartNode extends model.AgentFlowNode {
    /**
    * Extra configuration for the start node.
    */
    'startNodeConfig'?: {
        [key: string]: string;
    };
    "type": string;
}
export declare namespace StartNode {
    function getJsonObj(obj: StartNode, isParentJsonObj?: boolean): object;
    const type = "START_NODE";
    function getDeserializedJsonObj(obj: StartNode, isParentJsonObj?: boolean): object;
}
