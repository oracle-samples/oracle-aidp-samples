import * as model from '../model';
/**
* Result of listing tables in a schema.
*/
export interface TableCollection {
    /**
    * List of tables.
    */
    'items': Array<model.TableSummary>;
}
export declare namespace TableCollection {
    function getJsonObj(obj: TableCollection): object;
    function getDeserializedJsonObj(obj: TableCollection): object;
}
