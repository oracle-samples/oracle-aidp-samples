"use strict";
// Copyright (c) 2026, Oracle and/or its affiliates.  All rights reserved.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Table = void 0;
const model = __importStar(require("../model"));
const common = require("oci-common");
var Table;
(function (Table) {
    function getJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {
            'managedTableDefinition': obj.managedTableDefinition ?
                model.ManagedTableDefinition.getJsonObj(obj.managedTableDefinition) : undefined,
            'externalTableDefinition': obj.externalTableDefinition ?
                model.ExternalTableDefinition.getJsonObj(obj.externalTableDefinition) : undefined,
            'tableFields': obj.tableFields ?
                obj.tableFields.map((item) => { return model.TableFieldDetails.getJsonObj(item); })
                : undefined,
            'partitionKeys': obj.partitionKeys ?
                obj.partitionKeys.map((item) => { return model.TableFieldDetails.getJsonObj(item); })
                : undefined,
            'tableProperties': obj.tableProperties ?
                obj.tableProperties.map((item) => { return model.TableProperty.getJsonObj(item); })
                : undefined,
        });
        if (obj && "entityType" in obj && obj.entityType) {
            switch (obj.entityType) {
                case "ORACLE":
                    return model.OracleTable.getJsonObj(jsonObj, true);
                case "ADW":
                    return model.AdwTable.getJsonObj(jsonObj, true);
                case "ALH":
                    return model.AlhTable.getJsonObj(jsonObj, true);
                case "STANDARD":
                    return model.StandardTable.getJsonObj(jsonObj, true);
                case "EXADATA":
                    return model.ExadataTable.getJsonObj(jsonObj, true);
                case "ATP":
                    return model.AtpTable.getJsonObj(jsonObj, true);
                case "ORACLE_ANALYTICS":
                    return model.OacTable.getJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.entityType}`);
            }
        }
        return jsonObj;
    }
    Table.getJsonObj = getJsonObj;
    ;
    function getDeserializedJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {
            'managedTableDefinition': obj.managedTableDefinition ?
                model.ManagedTableDefinition.getDeserializedJsonObj(obj.managedTableDefinition) : undefined,
            'externalTableDefinition': obj.externalTableDefinition ?
                model.ExternalTableDefinition.getDeserializedJsonObj(obj.externalTableDefinition) : undefined,
            'tableFields': obj.tableFields ?
                obj.tableFields.map((item) => { return model.TableFieldDetails.getDeserializedJsonObj(item); })
                : undefined,
            'partitionKeys': obj.partitionKeys ?
                obj.partitionKeys.map((item) => { return model.TableFieldDetails.getDeserializedJsonObj(item); })
                : undefined,
            'tableProperties': obj.tableProperties ?
                obj.tableProperties.map((item) => { return model.TableProperty.getDeserializedJsonObj(item); })
                : undefined,
        });
        if (obj && "entityType" in obj && obj.entityType) {
            switch (obj.entityType) {
                case "ORACLE":
                    return model.OracleTable.getDeserializedJsonObj(jsonObj, true);
                case "ADW":
                    return model.AdwTable.getDeserializedJsonObj(jsonObj, true);
                case "ALH":
                    return model.AlhTable.getDeserializedJsonObj(jsonObj, true);
                case "STANDARD":
                    return model.StandardTable.getDeserializedJsonObj(jsonObj, true);
                case "EXADATA":
                    return model.ExadataTable.getDeserializedJsonObj(jsonObj, true);
                case "ATP":
                    return model.AtpTable.getDeserializedJsonObj(jsonObj, true);
                case "ORACLE_ANALYTICS":
                    return model.OacTable.getDeserializedJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.entityType}`);
            }
        }
        return jsonObj;
    }
    Table.getDeserializedJsonObj = getDeserializedJsonObj;
})(Table || (exports.Table = Table = {}));
//# sourceMappingURL=table.js.map