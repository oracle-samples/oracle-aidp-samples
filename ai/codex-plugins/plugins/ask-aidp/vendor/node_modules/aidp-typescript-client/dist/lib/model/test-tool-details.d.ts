/**
* Base model for testing any tool.  {@code toolType} drives the subtype.
*/
export interface TestToolDetails {
    /**
    * Agent flow id for which the tool is being tested
    */
    'agentFlowId': string;
    "toolType": string;
}
export declare namespace TestToolDetails {
    function getJsonObj(obj: TestToolDetails): object;
    function getDeserializedJsonObj(obj: TestToolDetails): object;
}
