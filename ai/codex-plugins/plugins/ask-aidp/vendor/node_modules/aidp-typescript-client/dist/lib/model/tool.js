"use strict";
// Copyright (c) 2026, Oracle and/or its affiliates.  All rights reserved.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Tool = void 0;
const model = __importStar(require("../model"));
const common = require("oci-common");
var Tool;
(function (Tool) {
    function getJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {});
        if (obj && "toolType" in obj && obj.toolType) {
            switch (obj.toolType) {
                case "CUSTOM":
                    return model.CustomTool.getJsonObj(jsonObj, true);
                case "PROMPT":
                    return model.PromptTool.getJsonObj(jsonObj, true);
                case "MCP":
                    return model.McpTool.getJsonObj(jsonObj, true);
                case "SQL":
                    return model.SqlTool.getJsonObj(jsonObj, true);
                case "RAG":
                    return model.RagTool.getJsonObj(jsonObj, true);
                case "HTTP":
                    return model.HttpTool.getJsonObj(jsonObj, true);
                case "NL2SQL":
                    return model.NlToSqlTool.getJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.toolType}`);
            }
        }
        return jsonObj;
    }
    Tool.getJsonObj = getJsonObj;
    ;
    function getDeserializedJsonObj(obj) {
        const jsonObj = Object.assign(Object.assign({}, obj), {});
        if (obj && "toolType" in obj && obj.toolType) {
            switch (obj.toolType) {
                case "CUSTOM":
                    return model.CustomTool.getDeserializedJsonObj(jsonObj, true);
                case "PROMPT":
                    return model.PromptTool.getDeserializedJsonObj(jsonObj, true);
                case "MCP":
                    return model.McpTool.getDeserializedJsonObj(jsonObj, true);
                case "SQL":
                    return model.SqlTool.getDeserializedJsonObj(jsonObj, true);
                case "RAG":
                    return model.RagTool.getDeserializedJsonObj(jsonObj, true);
                case "HTTP":
                    return model.HttpTool.getDeserializedJsonObj(jsonObj, true);
                case "NL2SQL":
                    return model.NlToSqlTool.getDeserializedJsonObj(jsonObj, true);
                default:
                    if (common.LOG.logger)
                        common.LOG.logger.info(`Unknown value for: ${obj.toolType}`);
            }
        }
        return jsonObj;
    }
    Tool.getDeserializedJsonObj = getDeserializedJsonObj;
})(Tool || (exports.Tool = Tool = {}));
//# sourceMappingURL=tool.js.map