/**
* The data to update a folder.
*/
export interface UpdateDirDetails {
    /**
    * The name of the volume folder. This will be the name of the folder in the volume.
*
    */
    'displayName'?: string;
}
export declare namespace UpdateDirDetails {
    function getJsonObj(obj: UpdateDirDetails): object;
    function getDeserializedJsonObj(obj: UpdateDirDetails): object;
}
