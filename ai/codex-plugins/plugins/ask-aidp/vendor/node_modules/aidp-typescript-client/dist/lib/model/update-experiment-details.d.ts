/**
* The data to update an experiment.
*/
export interface UpdateExperimentDetails {
    /**
    * Unique identifier for the experiment.
    */
    'experimentId': string;
    /**
    * If provided, name of the experiment is changed to the new name. The new name must be unique.
    */
    'newName'?: string;
}
export declare namespace UpdateExperimentDetails {
    function getJsonObj(obj: UpdateExperimentDetails): object;
    function getDeserializedJsonObj(obj: UpdateExperimentDetails): object;
}
