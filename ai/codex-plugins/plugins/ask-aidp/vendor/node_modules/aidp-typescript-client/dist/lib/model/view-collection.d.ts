import * as model from '../model';
/**
* Result of listing views in a schema.
*/
export interface ViewCollection {
    /**
    * List of views.
    */
    'items': Array<model.ViewSummary>;
}
export declare namespace ViewCollection {
    function getJsonObj(obj: ViewCollection): object;
    function getDeserializedJsonObj(obj: ViewCollection): object;
}
