import * as model from '../model';
/**
* Result of listing volumes
*/
export interface VolumeCollection {
    /**
    * List of volumes.
    */
    'items': Array<model.VolumeSummary>;
}
export declare namespace VolumeCollection {
    function getJsonObj(obj: VolumeCollection): object;
    function getDeserializedJsonObj(obj: VolumeCollection): object;
}
