import * as model from '../model';
/**
* Result of listing files in a volume.
*/
export interface VolumeFileCollection {
    /**
    * List of volume files.
    */
    'items': Array<model.VolumeFileSummary>;
}
export declare namespace VolumeFileCollection {
    function getJsonObj(obj: VolumeFileCollection): object;
    function getDeserializedJsonObj(obj: VolumeFileCollection): object;
}
